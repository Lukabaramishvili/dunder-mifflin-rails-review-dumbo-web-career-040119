<h2>List of Employees</h2>

<ul>
  <% @employees.each do |employee|%>
  <li><%=link_to "#{employee.first_name}" "#{employee.last_name}", employee%></li>
  <%end%>
</ul>

<br>
<p><%= button_to "Create New Employee", new_employee_path, method: :get%></p>
